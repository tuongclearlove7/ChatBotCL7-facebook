{
  "name": "myjson",
  "version": "1.0.0",
  "description": "json",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start":"json-server --watch data.json"
  },
  "author": "clearlove7",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.1"
  }
}
